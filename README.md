# Викторина: Режим марафона

Веб-приложение с викториной, где игроки отвечают на вопросы с ограничением по времени. Набранные очки зависят от скорости ответа и сложности вопроса, а лучшие результаты сохраняются в турнирной таблице. Проект реализуется на **Python/Flask** с использованием **SQLite**.

---

## Установка и запуск (Windows / PowerShell)

```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```
Инициализировать БД (в PowerShell, в корне проекта):
```bash
flask --app app init-db
```

Откройте в браузере: http://127.0.0.1:5000/

---

## API — POST /api/submit_answer
Описание. Сервер проверяет ответ игрока и начисляет очки в зависимости от скорости и сложности вопроса.

```
Endpoint: POST /api/submit_answer
```

```
Content-Type: application/json
```

```
Request
{
  "nickname": "Player1",
  "question_id": 15,
  "answer": "2",
  "time_left": 8.5
}
```

Response — 200 OK
```json
{
  "correct": true,
  "score": 85,
  "total_score": 420,
  "combo": 3,
  "next_question": {
    "id": 16,
    "text": "Сколько планет в Солнечной системе?",
    "answers": ["8", "9", "10", "7"],
    "difficulty": 2
  }
}
```

Пояснения:

- correct — правильность ответа
- score — очки за текущий вопрос
- total_score — общий счет игры
- combo — текущая серия правильных ответов
- next_question — следующий вопрос

---

## 👥Состав команды
- Чернышов Иван (GitHub: sfdms) — Backend (Flask + API)
- Мельникова Ирина (GitHub: capirmv) — Игровая логика + Frontend (JS + БД)
- Поликарпов Илья (GitHub: zipoo55) — Верстальщик / UI-дизайнер / Тесты

---

## Маршруты
- `/` — главная: ввод никнейма, игра, лидерборд
- `/rules` — правила
- API: `/api/start_game`, `/api/submit_answer`, `/api/leaderboard`

## Примечания
- Таймер: 10 секунд на вопрос
- Очки: 10×сложность + бонус за время + бонус комбо
- Результат сохраняется в ТОП-10 по окончании раунда

## Техническое задание

- [Техническое задание](technical-task.md)


